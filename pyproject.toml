[project]
name = "tts-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "google-cloud-texttospeech",
    "kokoro",
    "numpy",
    "python-docx",
    "pyyaml",
    "soundfile",
]
